// Cloud sync backend (Firebase project "li-lead-engine", free Spark plan).
// The apiKey is a public client identifier, not a secret — access is governed
// by Firestore security rules + the unguessable sync code.
window.SYNC_CONFIG = {
  apiKey: "AIzaSyBQpiWpPM-sim2Up5RG7NroKfrtePL43i0",
  projectId: "li-lead-engine"
};
